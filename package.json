{
    "private": true,
    "workspaces": [
        "mixture",
        "models",
        "ext-types",
        "globals",
        "endpoints",
        "react",
        "tests",
        "docs",
        "examples/*"
    ],
    "scripts": {
        "build": "yarn && yarn build:all && yarn test",
        "build:all": "yarn workspace @type-r/mixture build && yarn workspace @type-r/models build && yarn workspace @type-r/ext-types build && yarn workspace @type-r/globals build && yarn workspace @type-r/endpoints build && yarn workspace @type-r/react build && yarn workspace @type-r/tests build && yarn workspace @type-r/docs build",
        "test": "yarn workspace @type-r/tests test"
    },
    "devDependencies": {
        "rimraf": "^2.6.3",
        "rollup": "*",
        "rollup-plugin-commonjs": "*",
        "rollup-plugin-node-resolve": "*",
        "rollup-plugin-paths": "*",
        "rollup-plugin-sourcemaps": "*",
        "rollup-plugin-uglify": "*",
        "typescript": "6.0.3"
    }
}
